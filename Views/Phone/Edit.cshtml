@model WebApplication1.Models.Phone

@{
    ViewData["Title"] = "Редактировать телефон";
}

<h1>Редактировать телефон</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            @{ await Html.RenderPartialAsync("_MobileForm", Model);}
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад к списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
